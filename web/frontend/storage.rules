rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Ilustraciones del diccionario (solo lectura publica).
    match /corpus-img/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
